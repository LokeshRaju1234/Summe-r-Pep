class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        //bcz for every num subtracting with even - odd = oddnumber && odd - even = oddNumber
        //so for every number it is possible to make this either odd or even array from the array
        return true;
    }
};